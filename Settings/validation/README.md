# Settings Validation

This package contains validation-related settings that are part of the central
`Settings` object.

`holdout.py` preserves the holdout-validation contract used by optimization
validation and by the Learning runtime for explicit model or artifact overrides.
