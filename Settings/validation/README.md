# Settings validation

This package contains configuration dataclasses for validation-only runtime
overrides.  The settings composer imports this package directly, so it must be
tracked as source code and must not be treated like generated validation output.
