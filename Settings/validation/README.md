# Validation settings

This package contains explicit validation settings that are part of the global
`Settings` single source of truth.

Currently it holds the holdout override hook used by the learning resolver for
manual validation runs. Empty values mean that no forced model or artifact is
active.
