# Settings Validation

This package holds validation-related settings objects that are part of the
top-level `Settings` dataclass.

It exists as source configuration, not as runtime validation output; generated
validation artifacts stay under `Optimization/run/validation/`.
