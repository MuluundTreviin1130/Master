# Validation Settings

This package stores explicit validation/audit overrides for model resolution.

Defaults are intentionally empty: if no holdout `model_id` or `artifact_path`
is configured, Learning resolves models through the normal registry contract.
