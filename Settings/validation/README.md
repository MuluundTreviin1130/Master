# Validation settings

`Settings.validation` stores explicit validation-run controls that are part of
the repository Settings SSOT.

The current tracked contract is `validation.holdout.model_id`, used by validation
and smoke overrides to force a specific trained model artifact.
