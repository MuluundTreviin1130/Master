# Validation Settings

This package contains source-controlled settings for validation-only overrides.
Run artifacts remain in root-level or optimization validation output folders and
are not stored here.
