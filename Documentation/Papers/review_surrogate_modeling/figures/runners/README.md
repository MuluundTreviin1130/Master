# Figure runners

Reproducible scripts that build review figures. Paths are resolved relative to
the review root. Builders write PNG files to `../` and tabular outputs to
`../csv/`.

PDF export is intentionally disabled by default to keep the figure layer clean.
