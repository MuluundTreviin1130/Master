Test helpers for regression checks that can run without the full modeling
dependency stack. Keep these tests focused on critical runner behavior and
repository wiring that must remain valid in a fresh clone.
