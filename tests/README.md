This folder contains small dependency-light regression tests for critical repo
contracts. Prefer `python3 -m unittest discover -s tests` so the checks can run
on machines without optional optimization or ML packages installed.
