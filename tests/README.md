# tests

Focused correctness tests for Settings / Optimization / Learning contracts.

Prefer import-light unit tests that load specific modules without pulling the
full `Settings` / `Data` import chain when possible.
