# Legacy datafile_Umstellung

This folder documents the old `datafile_Umstellung` migration tree that used to
exist as a parallel codebase in the repository history.

Current status:

- the old tree no longer exists as a live workspace folder
- the active codebase now lives in the main repo root
- remaining references were Git index/history artifacts rather than active code

Intent:

- keep the old migration context conceptually in `legacy/`
- avoid treating the historical `datafile_Umstellung/` tree as part of the
  active repo structure
