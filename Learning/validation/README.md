# Learning Validation

This package contains small, reusable validation contracts for native learning
paths.  The code here is source-controlled; run-output validation folders remain
under `Optimization/run/validation/` and are ignored as artifacts.
