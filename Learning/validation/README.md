# Learning Validation

This package contains validation helpers for native Learning models.

`evaluate_gate.py` evaluates holdout predictions against the configured
Learning validation gate before a newly trained surrogate can become eligible.
