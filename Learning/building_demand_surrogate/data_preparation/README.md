# EnergyPlus Teacher Data Preparation

These scripts audit existing EnergyPlus teacher runs, build explicit Vienna
reference-week bridges and report whether heating and cooling targets are
ready for surrogate training.

Teacher tables are written to
`Learning/datasets/vienna_building_energyplus_teacher/`. Paper-facing review
tables are written to the Vienna paper's `Results/Model_Review/` folder.

